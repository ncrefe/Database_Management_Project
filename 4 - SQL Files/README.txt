IMPORTANT NOTE: Files should be run sequentially.
1-Creates the tables to be used in the database. Contains business rules.
2- Injects data population.
3- Contains codes that trigger triggers. It contains the data created for the test, so to use it, step 1 must be repeated and step 2 must be skipped.
   Observe the errors that the code gave at this point.